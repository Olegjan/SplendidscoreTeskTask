/**
 * GameConfig.ts
 * ---------------------------------------------------------------
 * Єдине місце, де живуть усі числа балансу гри.
 *
 * Навіщо окремий файл: щоб підкрутити складність можна було, не
 * читаючи ігрову логіку. Жоден інший скрипт не хардкодить ці
 * значення — інакше баланс розповзається по восьми файлах.
 *
 * Одиниці: сантиметри (світові одиниці Lens Studio) і секунди.
 * Осі: +X праворуч, +Y вгору, -Z вперед (від камери).
 * ---------------------------------------------------------------
 */

/**
 * X-координати трьох смуг. Індекс 1 — центральна, стартова.
 *
 * Числа малі, бо вся сцена живе в масштабі камери за замовчуванням:
 * камера стоїть на z=40 з вертикальним fov ~63°, тож на дистанції
 * гравця у кадр по горизонталі влазить лише ~28 см. Смуги ширші за
 * це просто не було б видно.
 */
export const LANE_X: number[] = [-9, 0, 9];

/** Скільки смуг у грі (щоб не писати LANE_X.length по коду). */
export const LANE_COUNT = LANE_X.length;

export const CONFIG = {
    // --- Геометрія доріжки ---
    /** Z, на якому стоїть гравець. Перешкоди їдуть до нього. */
    playerZ: 0,
    /** Z, на якому народжуються обʼєкти (далеко попереду, -Z = вперед). */
    spawnZ: -280,
    /** Z, за яким обʼєкт вже позаду камери (вона на z=40) і йде в пул. */
    despawnZ: 55,

    // --- Швидкість і складність ---
    /** Стартова швидкість наближення обʼєктів, см/с (~2.5 с на реакцію). */
    baseSpeed: 110,
    /** Стеля швидкості — без неї гра стає нечесною, а не складною. */
    maxSpeed: 250,
    /** Приріст швидкості за секунду гри. */
    speedRampPerSecond: 2.6,

    /** Інтервал між перешкодами на старті, с. */
    spawnIntervalStart: 1.35,
    /** Мінімальний інтервал — нижче цього ухилитись фізично не встигнути. */
    spawnIntervalMin: 0.55,
    /** На скільки секунд скорочується інтервал за кожну секунду гри. */
    spawnIntervalRampPerSecond: 0.012,
    /** Інтервал між монетками, с (живе окремо від перешкод). */
    coinInterval: 0.85,

    // --- Гравець ---
    /** Час переходу між смугами, с. Менше = різкіше керування. */
    laneChangeDuration: 0.16,
    /** Висота стрибка, см. */
    jumpHeight: 15,
    /** Повна тривалість дуги стрибка, с. */
    jumpDuration: 0.62,
    /** Тривалість підкату, с. */
    slideDuration: 0.55,
    /** У скільки разів стискається персонаж під час підкату. */
    slideScaleY: 0.45,

    // --- Зіткнення (перевірка в коді, не фізикою — див. TrackItem.ts) ---
    /** Піввисота зони зіткнення по Z, см. */
    hitHalfDepth: 8,
    /** Піввисота зони підбору монетки по Z, см. */
    coinHalfDepth: 9,
    /** Нижче цієї висоти стрибок ще не рятує від низької перешкоди, см. */
    jumpClearHeight: 6,

    // --- Життя ---
    maxLives: 3,
    /** Недоторканність після удару, с — щоб одна перешкода не зняла два життя. */
    hitInvulnerability: 0.9,

    // --- Рахунок ---
    scorePerCoin: 10,
    /** Бали за секунду виживання — винагорода за дистанцію, а не лише за монетки. */
    scorePerSecond: 2,

    // --- Пул обʼєктів ---
    obstaclePoolSize: 10,
    coinPoolSize: 14,
};

/** Затискає індекс смуги в допустимий діапазон [0, LANE_COUNT-1]. */
export function clampLane(index: number): number {
    return Math.max(0, Math.min(LANE_COUNT - 1, index));
}
